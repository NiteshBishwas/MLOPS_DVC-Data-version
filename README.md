# MLOPS_DVC-Data-version
This repository demonstrates data versioning using DVC (Data Version Control).  It showcases how large datasets and model files can be tracked, versioned, and  managed efficiently alongside code, without bloating the Git repository.
